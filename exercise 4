Грузовик с грузоподъемностью 1.1 тонн
Создание объекта Truck:

В методе Main создается объект класса Truck с параметрами 2 (количество сидений) и 1.1m (грузоподъемность в тоннах).

Конструктор Truck инициализирует свойства Seats (унаследованное от Auto) и Capacity.

Вывод на консоль:

В строке Console.WriteLine($"Грузовик с грузоподъемностью {truck.Capacity} тонн") используется интерполяция строк.

Значение truck.Capacity подставляется как 1.1, а "тонн" добавляется как часть строки.

Особенности:

Класс Truck наследует от Auto, но в данном коде свойство Seats не используется при выводе.

Суффикс m в 1.1m указывает, что это литерал типа decimal (а не double).

Программа работает корректно и выводит ожидаемый результат.
