Auto has been created
Truck has been created
Truck with capacity 1.1



Создание объекта Truck:

При создании объекта производного класса Truck сначала вызывается конструктор базового класса Auto.

В классе Truck не указано явно, какой конструктор базового класса использовать (с помощью : base(...)), поэтому компилятор автоматически вызывает конструктор по умолчанию базового класса (без параметров).

Конструктор Auto() выводит на консоль строку "Auto has been created".

Выполнение конструктора Truck:

После выполнения конструктора базового класса выполняется тело конструктора Truck(decimal capacity).

Устанавливаются свойства Seats = 2 и Capacity = capacity.

Выводится строка "Truck has been created".

Вывод информации о грузовике:

В методе Main после создания объекта truck выводится строка с его грузоподъемностью: "Truck with capacity 1.1".

Таким образом, вывод программы отражает порядок вызовов конструкторов и выполнение кода в них.
